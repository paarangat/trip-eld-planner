"""HOS scheduling algorithm.

Pure Python. Takes route legs + current cycle hours; returns a Timeline.
Implementations land in subsequent commits — the correctness rules in
CLAUDE.md §7 are the spec.
"""
